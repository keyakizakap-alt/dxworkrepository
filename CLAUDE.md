# CLAUDE.md

## 1. プロジェクト概要

このプロジェクトは、以下を目的とする。

- プロジェクト名: `<PROJECT_NAME>`
- 主な目的: `<PROJECT_PURPOSE>`
- 想定ユーザー: `<TARGET_USERS>`
- 実行環境: Windows / Linux / Docker / AWS / GCP
- 主要言語: Python / TypeScript / JavaScript / SQL
- AI関連技術: LLM / RAG / AI Agent / Dify / MCP
- データベース: PostgreSQL
- リポジトリ管理: GitHub

作業開始時は、README、既存コード、設定ファイル、テスト、ディレクトリ構成を確認し、プロジェクトの既存方針を優先すること。

---

## 2. 基本的な作業方針

以下の順序で作業すること。

1. ユーザーの目的を特定する
2. 関係するファイルを調査する
3. 既存実装と依存関係を確認する
4. 変更方針を簡潔に提示する
5. 必要最小限の変更を行う
6. テスト、Lint、型チェックを実行する
7. 変更内容と残存リスクを報告する

ユーザーが明示的に求めていない大規模なリファクタリングは行わないこと。

問題の原因を特定する前に、推測だけでコードを変更しないこと。

---

## 3. 回答言語と説明方法

- 原則として日本語で回答する
- コード、識別子、コマンド、ログは原文を維持する
- 結論、理由、変更内容、確認結果の順で説明する
- 不確かな内容は「仮説」「推定」「未確認」と明記する
- 複数案がある場合は、利点、欠点、推奨案を整理する
- 技術用語は使用してよいが、重要な箇所には短い補足を付ける
- 長い説明より、具体的なファイル名、コマンド、変更箇所を優先する

---

## 4. 実装前の確認事項

変更前に、可能な範囲で以下を確認すること。

- README.md
- package.json
- pyproject.toml
- requirements.txt
- Dockerfile
- compose.yaml
- docker-compose.yml
- .env.example
- tsconfig.json
- eslint.config.*
- biome.json
- pytest.ini
- GitHub Actions
- Terraform、CloudFormationなどのIaC
- データベースマイグレーション
- 既存テスト
- API仕様
- アーキテクチャ資料

既存の命名、設計パターン、ライブラリ、フォーマッターを優先し、独自ルールを不用意に持ち込まないこと。

---

## 5. 変更範囲の原則

- タスク達成に必要な最小差分を優先する
- 関係のないコードを変更しない
- ファイル全体の書き換えより、局所的な編集を優先する
- 公開APIや既存インターフェースを不用意に変更しない
- 後方互換性を壊す変更は事前に明示する
- 依存パッケージの追加は必要性を説明する
- 新規ライブラリ導入前に、既存依存で実現できないか確認する
- 自動生成ファイルは原則として直接編集しない

大きな変更が必要な場合は、以下に分割する。

1. 安全な準備変更
2. 本体実装
3. 移行処理
4. 不要コードの削除

---

## 6. コーディング規約

### 共通

- 可読性を優先する
- 関数とクラスは単一責任に近づける
- 重複コードを避ける
- マジックナンバーを定数化する
- エラーを握りつぶさない
- ログには原因調査に必要な情報を含める
- コメントは「何をしているか」より「なぜ必要か」を記述する
- 不要なコメントや自明なコメントは追加しない
- 既存コードのスタイルに合わせる

### 命名

- 名前から役割が分かるようにする
- data、tmp、value など曖昧な名前を多用しない
- 真偽値は is、has、can、should などを使用する
- 関数名は動詞から始める
- クラス名は責務を表す名詞にする

---

## 7. Python

- 型ヒントを付ける
- `Any` の使用は必要最小限にする
- データ構造には必要に応じて dataclass、Pydanticを利用する
- パス操作には原則 pathlib を使用する
- 外部I/Oにはタイムアウトを設定する
- 例外は具体的な型で捕捉する
- `except Exception` を使用する場合は、理由とログを残す
- 非同期処理ではブロッキングI/Oを混在させない
- 並列処理ではCPUバウンドとI/Oバウンドを区別する
- リソースはコンテキストマネージャーで確実に解放する

推奨チェック:

```bash
ruff check .
ruff format --check .
mypy .
pytest
```

プロジェクトに別のコマンドが定義されている場合は、そちらを優先すること。

---

## 8. TypeScript / JavaScript

- 新規実装は原則としてTypeScriptを使用する
- `any` を避ける
- API境界、外部入力、環境変数は実行時検証する
- 型アサーションで問題を隠さない
- 非同期処理ではエラーハンドリングを明示する
- Reactでは不要な状態と副作用を増やさない
- Server ComponentとClient Componentの境界を意識する
- クライアントへ秘密情報を渡さない
- Node.js専用処理をブラウザ側へ含めない

推奨チェック:

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

package.json に定義されたスクリプトを確認してから実行すること。

---

## 9. SQL・データベース

- SQLはパラメータ化し、文字列連結で組み立てない
- `SELECT *` を避ける
- 更新・削除条件を明示する
- 大量データ処理ではページングまたはバッチ処理を使う
- N+1クエリを避ける
- インデックス追加時は書き込み性能と容量への影響も考慮する
- トランザクション境界を明確にする
- マイグレーションは可能な限り後方互換にする
- 本番データを破壊する操作は実行しない
- DROP、TRUNCATE、条件なしのDELETEやUPDATEを実行しない

スキーマ変更時は以下を説明する。

- 変更理由
- 互換性
- 移行方法
- ロールバック方法
- ロックおよび性能への影響

---

## 10. API設計

- APIの入力と出力を明確に定義する
- 入力値をサーバー側で検証する
- 適切なHTTPステータスコードを使用する
- エラーレスポンス形式を統一する
- タイムアウト、リトライ、冪等性を考慮する
- 外部API障害時のフォールバックを検討する
- APIキーやトークンをログに出力しない
- レート制限を考慮する
- API仕様変更では後方互換性を確認する

外部APIのリトライは、無条件に行わず以下を考慮する。

- 冪等性
- 最大試行回数
- 指数バックオフ
- ジッター
- タイムアウト
- リトライ対象エラー

---

## 11. LLM・生成AI実装

LLMを利用する処理では、以下を考慮すること。

- システムプロンプトとユーザー入力を分離する
- ユーザー入力を命令として無条件に信頼しない
- Prompt Injectionを考慮する
- 構造化出力にはJSON Schemaまたは型付きモデルを使用する
- 出力を利用前に検証する
- モデル出力をそのままSQL、Shell、URL、ファイルパスとして実行しない
- モデル名、温度、最大トークン数、タイムアウトを設定可能にする
- モデル呼び出し失敗時の処理を実装する
- 入出力トークン数、レイテンシ、エラー率を観測可能にする
- 個人情報や機密情報を不要にモデルへ送信しない
- プロンプト変更時は評価データセットで比較する

LLM出力を使う処理では、次の流れを基本とする。

```
入力検証
  ↓
プロンプト構築
  ↓
LLM呼び出し
  ↓
構文検証
  ↓
意味検証
  ↓
安全性検証
  ↓
後続処理
```

---

## 12. RAG

RAG実装では以下を確認すること。

- 文書分割方式
- チャンクサイズ
- オーバーラップ
- メタデータ
- 埋め込みモデル
- 検索件数
- 類似度閾値
- ハイブリッド検索の要否
- リランキングの要否
- 引用元の保持
- アクセス権限
- データ更新方法

回答生成時は、取得文書に根拠がない内容を断定しない。

可能な場合は、回答とともに以下を返す。

- 参照文書
- ページまたはセクション
- 検索スコア
- 根拠となる抜粋
- 情報が不足している場合の明示

---

## 13. AIエージェント・MCP

AIエージェントやMCPツールを実装する場合は、以下を守る。

- ツールの責務を小さく保つ
- ツール名と説明を具体的にする
- 入力スキーマを厳密にする
- 書き込み系ツールと読み取り系ツールを分離する
- 削除、送信、公開、課金などの操作は明示的な承認を必要とする
- 外部入力からツール実行命令が注入される可能性を考慮する
- ツール結果を信頼せず検証する
- タイムアウトと最大実行回数を設定する
- 無限ループを防止する
- 操作ログと監査ログを記録する
- ツール失敗時に安全に停止する

エージェントに自由なShell実行権限や、本番環境への無制限アクセスを与えないこと。

---

## 14. Dify

Dify関連の変更では以下を確認する。

- WorkflowまたはChatflowの目的
- 入力変数と出力変数
- ノード間のデータ型
- 条件分岐
- 例外ルート
- タイムアウト
- 再試行
- LLMモデル設定
- Knowledge Retrieval設定
- Codeノードの安全性
- HTTP Requestノードの認証情報
- 環境変数
- DSLのバージョン互換性

複雑な条件分岐を増やしすぎず、以下の単位に分ける。

1. 入力検証
2. 情報抽出
3. 検索・外部連携
4. 判定
5. 回答生成
6. 出力検証
7. エラー処理

---

## 15. AWS・クラウド

AWS関連の変更では以下を確認する。

- リージョン
- アカウント
- VPC
- サブネット
- セキュリティグループ
- IAMロール
- KMS
- Secrets ManagerまたはParameter Store
- CloudWatch Logs
- CloudTrail
- バックアップ
- 可用性
- コスト
- 障害時の復旧方法

設計提案では必ず以下を整理する。

- 利点
- 欠点
- コスト
- 可用性
- 運用負荷
- セキュリティ
- 代替案

IAMは最小権限とし、以下を避ける。

```json
{
  "Effect": "Allow",
  "Action": "*",
  "Resource": "*"
}
```

やむを得ず広い権限が必要な場合は、理由、対象環境、期限、縮小方法を説明する。

---

## 16. Docker

- 可能な限り軽量なベースイメージを使用する
- ベースイメージのバージョンを固定する
- マルチステージビルドを検討する
- rootユーザーで実行しない
- 秘密情報をイメージに含めない
- `.dockerignore` を設定する
- 不要なパッケージやキャッシュを残さない
- ヘルスチェックを検討する
- コンテナをステートレスに保つ
- 永続データはボリュームまたは外部サービスに保存する

`latest` タグは原則使用しない。

---

## 17. Windows環境

このプロジェクトはWindowsで操作される可能性がある。

- PowerShellとBashの違いを考慮する
- WindowsパスとPOSIXパスを混在させない
- 改行コードの差異を考慮する
- パス区切り文字をハードコードしない
- ファイル名の大文字・小文字依存を避ける
- PowerShellコマンドを提示する場合はPowerShell用であることを明記する
- WSL、Docker Desktop、ネイティブWindowsのどれを前提にしているか明記する

PowerShell例:

```powershell
python -m pytest
npm run build
docker compose up -d
```

Bash例:

```bash
python -m pytest
npm run build
docker compose up -d
```

---

## 18. セキュリティ

以下の情報をコード、ログ、回答、コミットに含めない。

- APIキー
- パスワード
- アクセストークン
- シークレット
- 秘密鍵
- セッショントークン
- 個人情報
- 本番データ
- 接続文字列
- AWSアクセスキー

`.env` の値を読み取っても、回答にそのまま表示しない。

秘密情報が検出された場合は、値を伏せて以下を案内する。

1. 漏えいした認証情報の失効
2. 新しい認証情報の発行
3. Git履歴からの削除
4. Secret Manager等への移行
5. 影響範囲の監査

ユーザー入力、LLM出力、外部API結果、アップロードファイルは、すべて信頼できない入力として扱う。

---

## 19. 禁止事項

明示的な許可がない限り、以下を実行しない。

- 本番環境へのデプロイ
- 本番データベースへの接続
- 本番データの変更または削除
- Gitへのpush
- Pull Requestの作成
- リリースの実行
- メールやメッセージの送信
- 課金が発生するリソースの作成
- クラウドリソースの削除
- IAM権限の拡大
- シークレットの変更
- `git reset --hard`
- `git clean -fd`
- 強制push
- 条件なしのデータ更新・削除
- OS全体へ影響する設定変更

危険なコマンドを提案する場合は、影響範囲と安全な代替コマンドを併記する。

---

## 20. Git運用

- 作業開始時に現在のブランチと変更状況を確認する
- ユーザーの未コミット変更を上書きしない
- 関係のない変更を元に戻さない
- コミットは論理的な単位に分ける
- コミットメッセージは変更理由が分かるようにする
- 自動生成署名や不要な文言を追加しない
- push、merge、rebaseは明示的な依頼がある場合のみ行う

推奨コミット形式:

```
<type>(<scope>): <summary>
```

例:

```
feat(api): add product recommendation endpoint
fix(rag): prevent empty retrieval results from reaching the LLM
test(auth): add expired-token test cases
docs(architecture): document MCP request flow
```

---

## 21. テスト方針

変更内容に応じてテストを追加または更新する。

最低限、以下を検討する。

- 正常系
- 異常系
- 境界値
- 空入力
- 不正な型
- タイムアウト
- 外部API失敗
- 権限不足
- 重複実行
- 並列実行
- 大量データ
- セキュリティ上の不正入力

バグ修正では、可能な限り再現テストを先に追加する。

テストを実行できない場合は、実行していないことと理由を明記する。

---

## 22. パフォーマンス

以下の問題がないか確認する。

- N+1クエリ
- 不要なAPI呼び出し
- 逐次実行できる処理の過剰な並列化
- 並列化できる独立処理の不要な直列化
- メモリへの全件読み込み
- 無制限なキャッシュ
- 無制限なリトライ
- 巨大なログ
- 不要な再レンダリング
- 大きなLLMコンテキスト
- 重複した埋め込み生成

最適化は計測結果に基づいて行い、可読性を大幅に損なう早すぎる最適化は避ける。

---

## 23. ログ・監視

ログには以下を含める。

- タイムスタンプ
- ログレベル
- 処理名
- リクエストIDまたはトレースID
- エラー種別
- 処理時間
- 必要に応じて外部サービス名

以下はログに含めない。

- パスワード
- アクセストークン
- Cookie
- Authorizationヘッダー
- 個人情報
- LLMへ渡した機密プロンプト
- 機密文書の全文

監視では、可能な範囲で以下を検討する。

- エラー率
- レイテンシ
- スループット
- CPU
- メモリ
- ディスク
- キュー滞留
- 外部API失敗率
- LLMトークン数
- LLMコスト
- RAG検索失敗率
- 不正アクセス

---

## 24. ドキュメント

挙動、構築方法、設定、API、環境変数を変更した場合は、関連ドキュメントも更新する。

READMEには最低限、以下を含める。

- プロジェクト概要
- 前提条件
- セットアップ手順
- 起動方法
- テスト方法
- 環境変数
- ディレクトリ構成
- トラブルシューティング
- デプロイ方法
- セキュリティ上の注意

コードと矛盾するドキュメントを残さない。

---

## 25. アーキテクチャ提案

アーキテクチャを提案する場合は、以下の形式で整理する。

### 要件

- 機能要件
- 非機能要件
- 制約
- 前提

### コンポーネント

| コンポーネント | 役割 | 入力 | 出力 | 依存先 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### データフロー

```
Client
  ↓
API
  ↓
Validation
  ↓
Application Service
  ├─ Database
  ├─ External API
  └─ LLM / RAG
  ↓
Response
```

### トレードオフ

| 観点 | 推奨案 | 利点 | 欠点 |
| --- | --- | --- | --- |
| コスト |  |  |  |
| 可用性 |  |  |  |
| 運用負荷 |  |  |  |
| 性能 |  |  |  |
| セキュリティ |  |  |  |

---

## 26. コードレビュー

レビュー時は、以下の優先順位で確認する。

1. 重大なバグ
2. セキュリティ
3. データ破損
4. 互換性
5. 並行処理
6. エラーハンドリング
7. パフォーマンス
8. テスト不足
9. 保守性
10. スタイル

指摘には以下を含める。

- 重要度
- 対象ファイルと行
- 問題
- 発生条件
- 影響
- 修正案

単なる要約ではなく、実際に問題となる箇所を優先する。

---

## 27. タスク完了時の報告形式

作業完了時は、原則として以下の形式で報告する。

### 実施内容

- 変更した内容
- 変更したファイル

### 確認結果

- 実行したテスト
- Lint
- 型チェック
- ビルド

### 未確認事項

- 実行できなかった確認
- 残存するリスク
- 仮定した事項

### 次のアクション

- ユーザーが次に確認すべきこと
- 必要なデプロイまたは環境設定

---

## 28. 判断に迷った場合

以下の場合は、勝手に進めずユーザーへ確認する。

- 要件によって実装方式が大きく変わる
- 公開APIを破壊する可能性がある
- データ移行が必要
- 追加費用が発生する
- 本番環境へ影響する
- セキュリティ要件が不明
- 複数の設計案に大きなトレードオフがある
- 削除対象が本当に不要か判断できない

ただし、読み取り、調査、安全なローカルテストは可能な範囲で先に行い、確認事項を具体化すること。

---

## 29. プロジェクト固有情報

以下はプロジェクトごとに更新すること。

### ディレクトリ構成

```
<PROJECT_ROOT>/
├─ src/
├─ tests/
├─ docs/
├─ scripts/
├─ infra/
├─ .claude/
├─ CLAUDE.md
└─ README.md
```

### 主要コマンド

```bash
# セットアップ
<SETUP_COMMAND>
# 開発サーバー
<DEV_COMMAND>
# テスト
<TEST_COMMAND>
# Lint
<LINT_COMMAND>
# 型チェック
<TYPECHECK_COMMAND>
# ビルド
<BUILD_COMMAND>
```

### 環境

| 環境 | 用途 | デプロイ方法 |
| --- | --- | --- |
| local | ローカル開発 | 手動 |
| development | 結合確認 | CI/CD |
| staging | 本番前確認 | CI/CD |
| production | 本番 | 承認付きCI/CD |

### 重要な設計判断

- `<DECISION_1>`
- `<DECISION_2>`
- `<DECISION_3>`

### 変更禁止領域

- `<DO_NOT_EDIT_PATH>`
- `<GENERATED_FILES>`
- `<LEGACY_COMPONENT>`

### 参照ドキュメント

- README.md
- docs/architecture.md
- docs/api.md
- docs/database.md
- docs/security.md
